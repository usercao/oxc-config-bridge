{
  "name": "oxc-config-bridge",
  "version": "0.2.0",
  "description": "Run Oxlint and Oxfmt from one type-safe config without requiring Vite+",
  "keywords": [
    "oxc",
    "oxfmt",
    "oxlint",
    "config",
    "formatter",
    "linter"
  ],
  "homepage": "https://github.com/usercao/oxc-config-bridge#readme",
  "bugs": {
    "url": "https://github.com/usercao/oxc-config-bridge/issues"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/usercao/oxc-config-bridge.git"
  },
  "license": "MIT",
  "author": "usercao",
  "type": "module",
  "sideEffects": false,
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "bin": {
    "oxc-config-bridge": "dist/cli.js"
  },
  "files": [
    "dist",
    "README.md"
  ],
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js",
      "default": "./dist/index.js"
    },
    "./package.json": "./package.json"
  },
  "scripts": {
    "build": "node --input-type=module --eval \"import { rm } from 'node:fs/promises'; await rm('dist', { recursive: true, force: true })\" && tsc -p tsconfig.json",
    "check": "npm test && npm pack --dry-run",
    "prepack": "npm run build",
    "prepublishOnly": "npm test",
    "test": "tsc -p tsconfig.json && tsc -p tsconfig.test.json --noEmit && vitest run"
  },
  "devDependencies": {
    "@types/node": "^24.13.3",
    "oxfmt": "0.62.0",
    "oxlint": "1.77.0",
    "typescript": "~6.0.2",
    "vitest": "^3.2.4"
  },
  "peerDependencies": {
    "oxfmt": ">=0.62.0",
    "oxlint": ">=1.77.0"
  },
  "engines": {
    "node": ">=22.18.0"
  },
  "publishConfig": {
    "access": "public",
    "registry": "https://registry.npmjs.org/"
  },
  "packageManager": "npm@11.17.0"
}
